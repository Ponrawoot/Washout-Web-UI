import React from "react";
import ReactDOM from "react-dom";
import '../branch/LogIn.css';
import LogIN from './page';

ReactDOM.render(
    <React.StrictMode>
      <LogIN />
    </React.StrictMode>,
    document.getElementById('root')
  );